package jlatex.layout;

import jlatex.util.LatexContent;

/**
 * Represents a measurement element.  It does not add any new methods
 * to the LatexContent interface, but instead is a simple base interface
 * for all measurements.
 * 
 * @author Michael Oland
 *
 */
public interface LatexMeasurementElement extends LatexContent
{
}